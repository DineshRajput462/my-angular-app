import { ChangeBgColorDirective } from './change-bgcolor.directive';

describe('ChangeBgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBgColorDirective();
    expect(directive).toBeTruthy();
  });
});
